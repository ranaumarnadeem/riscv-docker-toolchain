#include <stdio.h>

int main() {
    float a = 1.5f;
    float b = 2.5f;
    float c = a * b; // Verification of F extension
    
    // We are just testing compilation, not running it yet
    return (int)c;
}